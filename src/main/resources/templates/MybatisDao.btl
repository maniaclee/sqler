package ${lee.getDaoPackage(config.pack)};

import ${lee.getEntityPackage(config.pack)}.${lee.getEntityClassName(table)}
/**
 * Created by ${config.author} on ${date(),"yyyy-MM-dd"} .
 */
public interface ${lee.getDaoClassName(table)} {
	${table.primaryKeyField.appType} add(${lee.getEntityClassName(table)} entity);
}
